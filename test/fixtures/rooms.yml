# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

roundtable:
  name: Round Table
  password_digest: <%= Room.digest('password') %>
  url_token: <%= SecureRandom.hex(10) %>

seraph:
  name: SE.RA.PH
  password_digest: <%= Room.digest('password') %>
  url_token: <%= SecureRandom.hex(10) %>